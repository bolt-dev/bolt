export MOZCONFIG=~/mozconfig.common
export MOZBUILD_STATE_PATH=~/
echo The MOZCONFIG is ${MOZCONFIG}
echo HOME: ${HOME} BUILD_HOME: ${BUILD_HOME}
cd ~/../comm/
echo Start building in $PWD with MOZ_MSVCBITS:${MOZ_MSVCBITS} 
export BUILD_TRIPLE=$BUILD_ARCH-$BUILD_VENDOR
export TARGET_DIR=$BUILD_HOME/Obj-$TARGET_NAME-$BUILD_TRIPLE-$BUILD_VARIANT
echo Config with PWD:$PWD TARGET_DIR: ${TARGET_DIR} BUILD_TRIPLE:$BUILD_TRIPLE TARGET_DIR:$TARGET_DIR

./mozilla/mach build
exit
